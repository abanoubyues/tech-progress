{
  "name": "bootdev-progress",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Live dashboard for boot.dev Back-end Developer Path progress",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "cf:dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
